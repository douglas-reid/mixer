subject: global
adapters:
  - name: default
    kind: quotas
    impl: memQuota
    params:
  - name: sdtrace
    kind: access-logs
    impl: sdtrace
    params:
      project_id: "kubernetes-hello-world-1385"
      # api_key: "AIzaSyCO1ahgCHbU3y_aiT9OmkCS9EU0yfIgSFM"
      #
      # $ ln -s creds-file.json adapter/sdtrace/svc-acct-creds.json
      # service_account_file: "adapter/sdtrace/svc-acct-creds.json"
  - name: default
    impl: stdioLogger
    params:
      logStream: STDERR
  - name: prometheus
    kind: metrics
    impl: prometheus
    params:
  - name: default
    impl: denyChecker
  - name: default
    kind: attributes
    impl: kubernetes
    params:
      # when running from mixer root, use the following config after adding a
      # symbolic link to a kubernetes config file via:
      #
      # $ ln -s ~/.kube/config adapter/kubernetes/kubeconfig
      #
      # kubeconfig_path: "adapter/kubernetes/kubeconfig"
